PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# SMTP settings for nodemailer
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
SMTP_FROM="Proof Contact" <no-reply@yourdomain.com>
# Where contact messages are delivered. Falls back to SMTP_FROM if not set.
SMTP_TO=taha.daoud03@gmail.com

# Frontend config
VITE_CONTACT_API=/api/contact